import React from 'react'
import styles from './SeriesSection.module.css'

const seriesSection = ({ children }) => (
  <section className={styles.SeriesSection}>{children}</section>
)

export default seriesSection
